# CLI-specific requirements
# For a complete list of all dependencies, see ../../requirements.txt

# Core Azure Key Vault dependencies (minimum needed for CLI)
azure-identity>=1.24.0
azure-keyvault-secrets>=4.10.0
azure-keyvault-keys>=4.11.0
azure-keyvault-certificates>=4.10.0

# CLI enhancements
click>=8.0.0
colorama>=0.4.0
tabulate>=0.9.0
